description: API rate limit exceeded response
content:
  application/json:
    schema:
      $ref: "../../schemas/error.yaml"
    example:
      code: too_many_requests
      message: API rate limit exceeded